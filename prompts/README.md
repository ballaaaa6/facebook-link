# Prompt Registry

Prompts are versioned product artifacts. Never edit an active prompt in place; add a new version and record the prompt version on each content or strategy artifact.

Each prompt must define inputs, output contract, prohibited behavior, and review requirements. Browser automation inserts sanitized structured input into these templates.
