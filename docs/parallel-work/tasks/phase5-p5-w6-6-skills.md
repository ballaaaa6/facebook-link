# P5-W6.6 — Project workflow skills

## Assignment

- Phase: **Phase 5 — Reproducible asset factory**
- Wave: `P5-W6-03`
- Base commit: `1a020ac`
- Worker: Session 6; Main records the generated branch/worktree at dispatch.
- Objective: create the three concise repository skills that route future
  Office V2 asset work to the real factory, board, registry, and canonical
  contracts.

## Required skill-creator workflow

- Follow the installed `skill-creator` instructions at
  `C:\Users\WINDOW XI\.codex\skills\.system\skill-creator\SKILL.md`.
- Use its `init_skill.py` scaffold and `quick_validate.py` validator for each
  skill. Keep each `SKILL.md` concise, imperative, and task-specific. No
  README, install guide, copied reference prose, or placeholder tool names.

## Required skills

- `.agents/skills/author-office-v2-asset-family/SKILL.md`: source/provenance /
  recipe/factory/board/registry workflow and fail-closed admission.
- `.agents/skills/compose-office-v2-room/SKILL.md`: compose a room through
  authoritative world/geometry/scene-bundle contracts without putting
  occupancy or interaction truth in pixels.
- `.agents/skills/review-office-v2-visuals/SKILL.md`: inspect generated review
  boards, record geometry/visual/commercial outcomes explicitly, and never
  infer approval from technical output.

## Owned files and forbidden behavior

- Own only the three skill directories under `.agents/skills/` and any required
  `agents/openai.yaml` metadata produced by the scaffold. Do not edit scripts,
  assets, schemas, manifests, package metadata, backlog, or final reports.
- Point to real repository paths and commands: factory, admission, boards,
  registry, canonical geometry/connectivity docs, and `npm run check`.
- Do not claim a family is approved or Phase 5 is closed.

## Acceptance and handoff

- All three skills pass `quick_validate.py`, contain valid frontmatter, use
  imperative instructions, and mention the canonical contracts/tools they
  actually call. Project skill preflight and full `npm run check` pass.
- Commit only the owned skill files, report exact commit/validation/clean
  worktree, then stop for Main review.
