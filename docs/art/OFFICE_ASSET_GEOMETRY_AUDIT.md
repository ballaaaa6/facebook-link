# Office Asset Geometry Audit

Status: Reviewed Geometry v3 inventory

This report is generated from the Office manifests, actual image files,
and `assets/game/manifests/office-asset-geometry-review.json`. Do not edit
this file directly; update the reviewed input and regenerate both outputs.

## Coverage

- Total inventory records: 297
- Distinct asset keys: 235
- Missing layer declarations: 35
- Missing referenced image files: 0
- Unreviewed records: 0

### Records by source

| Source | Records |
| --- | ---: |
| `character` | 19 |
| `library` | 212 |
| `planned` | 16 |
| `runtime` | 50 |

### Dispositions

| Disposition | Records |
| --- | ---: |
| `blocked-by-license` | 19 |
| `blocked-by-orientation` | 32 |
| `derive-composite` | 77 |
| `metadata-fix` | 142 |
| `regenerate` | 7 |
| `reuse` | 20 |

## Review conclusions

- Existing pixels are retained whenever their visible geometry supports the
  intended plane, base, and orientation.
- Straight-front workstation desks require regeneration because their
  bitmaps cannot express the accepted `5 x 4` top-down footprint.
- Surface furniture with usable pixels is assigned `derive-composite` when
  support, underframe, or foreground parts can be separated deterministically.
- Contact-sheet pixel cleanup is recorded as a reviewed override when an
  intended sprite is usable but its processed crop contains detached pixels
  from an adjacent source-sheet cell.
- The env-12 and env-13 side sheets are blocked because the side silhouettes
  do not form faithful turnarounds of their front families.
- Character pixels remain prototype-only and blocked independently by
  `pending-commercial-review`; detailed pose-anchor calibration is deferred
  until accepted furniture exists.

## Contact sheets

- `assets/game/processed/office-geometry-audit-v1/contact-sheets/characters-page-01.png`
- `assets/game/processed/office-geometry-audit-v1/contact-sheets/characters-page-02.png`
- `assets/game/processed/office-geometry-audit-v1/contact-sheets/library-chair-office-modern-v1.png`
- `assets/game/processed/office-geometry-audit-v1/contact-sheets/library-env-01-workstation-static.png`
- `assets/game/processed/office-geometry-audit-v1/contact-sheets/library-env-02-screen-themes.png`
- `assets/game/processed/office-geometry-audit-v1/contact-sheets/library-env-03-research-creative.png`
- `assets/game/processed/office-geometry-audit-v1/contact-sheets/library-env-04-release-noc.png`
- `assets/game/processed/office-geometry-audit-v1/contact-sheets/library-env-05-facility-lounge.png`
- `assets/game/processed/office-geometry-audit-v1/contact-sheets/library-env-06-decor-architecture-tv.png`
- `assets/game/processed/office-geometry-audit-v1/contact-sheets/library-env-07-animated-mechanical.png`
- `assets/game/processed/office-geometry-audit-v1/contact-sheets/library-env-08-animated-ambient.png`
- `assets/game/processed/office-geometry-audit-v1/contact-sheets/library-env-09-phase2-completion-architecture.png`
- `assets/game/processed/office-geometry-audit-v1/contact-sheets/library-env-10-storage-operations-detail.png`
- `assets/game/processed/office-geometry-audit-v1/contact-sheets/library-env-11-comfort-personal-detail.png`
- `assets/game/processed/office-geometry-audit-v1/contact-sheets/library-env-12-facility-side-orientations.png`
- `assets/game/processed/office-geometry-audit-v1/contact-sheets/library-env-13-lounge-storage-side-orientations.png`
- `assets/game/processed/office-geometry-audit-v1/contact-sheets/runtime-page-01.png`
- `assets/game/processed/office-geometry-audit-v1/contact-sheets/runtime-page-02.png`
- `assets/game/processed/office-geometry-audit-v1/contact-sheets/runtime-page-03.png`
- `assets/game/processed/office-geometry-audit-v1/contact-sheets/runtime-page-04.png`

## Character records

| Record | Type | Plane | Disposition | Visual finding | Reason |
| --- | --- | --- | --- | --- | --- |
| `character:ai-workbot` | `character` | `floor` | `blocked-by-license` | The runtime WebP exists and its first-frame alpha bounds were measured; furniture-specific pose anchors remain deferred. | Keep as an internal prototype only; pending-commercial-review blocks commercial activation independently of geometry. |
| `character:anna` | `character` | `floor` | `blocked-by-license` | The runtime WebP exists and its first-frame alpha bounds were measured; furniture-specific pose anchors remain deferred. | Keep as an internal prototype only; pending-commercial-review blocks commercial activation independently of geometry. |
| `character:asuka-2` | `character` | `floor` | `blocked-by-license` | The runtime WebP exists and its first-frame alpha bounds were measured; furniture-specific pose anchors remain deferred. | Keep as an internal prototype only; pending-commercial-review blocks commercial activation independently of geometry. |
| `character:baobao-2` | `character` | `floor` | `blocked-by-license` | The runtime WebP exists and its first-frame alpha bounds were measured; furniture-specific pose anchors remain deferred. | Keep as an internal prototype only; pending-commercial-review blocks commercial activation independently of geometry. |
| `character:boba` | `character` | `floor` | `blocked-by-license` | The runtime WebP exists and its first-frame alpha bounds were measured; furniture-specific pose anchors remain deferred. | Keep as an internal prototype only; pending-commercial-review blocks commercial activation independently of geometry. |
| `character:doraemon` | `character` | `floor` | `blocked-by-license` | The runtime WebP exists and its first-frame alpha bounds were measured; furniture-specific pose anchors remain deferred. | Keep as an internal prototype only; pending-commercial-review blocks commercial activation independently of geometry. |
| `character:einstein` | `character` | `floor` | `blocked-by-license` | The runtime WebP exists and its first-frame alpha bounds were measured; furniture-specific pose anchors remain deferred. | Keep as an internal prototype only; pending-commercial-review blocks commercial activation independently of geometry. |
| `character:gugugaga` | `character` | `floor` | `blocked-by-license` | The runtime WebP exists and its first-frame alpha bounds were measured; furniture-specific pose anchors remain deferred. | Keep as an internal prototype only; pending-commercial-review blocks commercial activation independently of geometry. |
| `character:itachi` | `character` | `floor` | `blocked-by-license` | The runtime WebP exists and its first-frame alpha bounds were measured; furniture-specific pose anchors remain deferred. | Keep as an internal prototype only; pending-commercial-review blocks commercial activation independently of geometry. |
| `character:jesus` | `character` | `floor` | `blocked-by-license` | The runtime WebP exists and its first-frame alpha bounds were measured; furniture-specific pose anchors remain deferred. | Keep as an internal prototype only; pending-commercial-review blocks commercial activation independently of geometry. |
| `character:lian-3` | `character` | `floor` | `blocked-by-license` | The runtime WebP exists and its first-frame alpha bounds were measured; furniture-specific pose anchors remain deferred. | Keep as an internal prototype only; pending-commercial-review blocks commercial activation independently of geometry. |
| `character:miku` | `character` | `floor` | `blocked-by-license` | The runtime WebP exists and its first-frame alpha bounds were measured; furniture-specific pose anchors remain deferred. | Keep as an internal prototype only; pending-commercial-review blocks commercial activation independently of geometry. |
| `character:nai-long` | `character` | `floor` | `blocked-by-license` | The runtime WebP exists and its first-frame alpha bounds were measured; furniture-specific pose anchors remain deferred. | Keep as an internal prototype only; pending-commercial-review blocks commercial activation independently of geometry. |
| `character:noir-webling` | `character` | `floor` | `blocked-by-license` | The runtime WebP exists and its first-frame alpha bounds were measured; furniture-specific pose anchors remain deferred. | Keep as an internal prototype only; pending-commercial-review blocks commercial activation independently of geometry. |
| `character:qq-penguin` | `character` | `floor` | `blocked-by-license` | The runtime WebP exists and its first-frame alpha bounds were measured; furniture-specific pose anchors remain deferred. | Keep as an internal prototype only; pending-commercial-review blocks commercial activation independently of geometry. |
| `character:rem-xl` | `character` | `floor` | `blocked-by-license` | The runtime WebP exists and its first-frame alpha bounds were measured; furniture-specific pose anchors remain deferred. | Keep as an internal prototype only; pending-commercial-review blocks commercial activation independently of geometry. |
| `character:ruri` | `character` | `floor` | `blocked-by-license` | The runtime WebP exists and its first-frame alpha bounds were measured; furniture-specific pose anchors remain deferred. | Keep as an internal prototype only; pending-commercial-review blocks commercial activation independently of geometry. |
| `character:taffy-2` | `character` | `floor` | `blocked-by-license` | The runtime WebP exists and its first-frame alpha bounds were measured; furniture-specific pose anchors remain deferred. | Keep as an internal prototype only; pending-commercial-review blocks commercial activation independently of geometry. |
| `character:yinyue-2` | `character` | `floor` | `blocked-by-license` | The runtime WebP exists and its first-frame alpha bounds were measured; furniture-specific pose anchors remain deferred. | Keep as an internal prototype only; pending-commercial-review blocks commercial activation independently of geometry. |

## Library records

| Record | Type | Plane | Disposition | Visual finding | Reason |
| --- | --- | --- | --- | --- | --- |
| `library:chair-office-modern-v1:chair.office.modern.back` | `seat` | `floor` | `metadata-fix` | Reviewed the turnaround: all four chair views share one silhouette family and expose a consistent seat/base relationship. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:chair-office-modern-v1:chair.office.modern.front` | `seat` | `floor` | `metadata-fix` | Reviewed the turnaround: all four chair views share one silhouette family and expose a consistent seat/base relationship. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:chair-office-modern-v1:chair.office.modern.side-left` | `seat` | `floor` | `metadata-fix` | Reviewed the turnaround: all four chair views share one silhouette family and expose a consistent seat/base relationship. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:chair-office-modern-v1:chair.office.modern.side-right` | `seat` | `floor` | `metadata-fix` | Reviewed the turnaround: all four chair views share one silhouette family and expose a consistent seat/base relationship. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-01-workstation-static:cable.tray` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: desks are straight-front and lack canonical depth; the remaining equipment and props have separable bases. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-01-workstation-static:cup.coffee` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: desks are straight-front and lack canonical depth; the remaining equipment and props have separable bases. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-01-workstation-static:desk.workstation.back` | `surface-furniture` | `floor` | `regenerate` | Reviewed the source sheet: desks are straight-front and lack canonical depth; the remaining equipment and props have separable bases. | Regenerate after Step 4 because the visible bitmap cannot represent the approved 5 x 4 footprint and 5 x 3 support plane. |
| `library:env-01-workstation-static:desk.workstation.front` | `surface-furniture` | `floor` | `regenerate` | Reviewed the source sheet: desks are straight-front and lack canonical depth; the remaining equipment and props have separable bases. | Regenerate after Step 4 because the visible bitmap cannot represent the approved 5 x 4 footprint and 5 x 3 support plane. |
| `library:env-01-workstation-static:keyboard.mouse` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: desks are straight-front and lack canonical depth; the remaining equipment and props have separable bases. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-01-workstation-static:lamp.desk` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: desks are straight-front and lack canonical depth; the remaining equipment and props have separable bases. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-01-workstation-static:laptop.open` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: desks are straight-front and lack canonical depth; the remaining equipment and props have separable bases. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-01-workstation-static:monitor.back` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: desks are straight-front and lack canonical depth; the remaining equipment and props have separable bases. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-01-workstation-static:monitor.front` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: desks are straight-front and lack canonical depth; the remaining equipment and props have separable bases. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-01-workstation-static:papers.stack` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: desks are straight-front and lack canonical depth; the remaining equipment and props have separable bases. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-01-workstation-static:pc.case` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the source sheet: desks are straight-front and lack canonical depth; the remaining equipment and props have separable bases. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-01-workstation-static:phone.desk` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: desks are straight-front and lack canonical depth; the remaining equipment and props have separable bases. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-01-workstation-static:plant.small` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the source sheet: desks are straight-front and lack canonical depth; the remaining equipment and props have separable bases. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-01-workstation-static:power.strip` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: desks are straight-front and lack canonical depth; the remaining equipment and props have separable bases. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-01-workstation-static:speaker.desktop` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: desks are straight-front and lack canonical depth; the remaining equipment and props have separable bases. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. The missing layer declaration must be corrected. |
| `library:env-01-workstation-static:tablet.drawing` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: desks are straight-front and lack canonical depth; the remaining equipment and props have separable bases. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-02-screen-themes:screen.theme.content.a` | `animated-shell` | `wall` | `derive-composite` | The overlay itself is coherent, but the processed crop contains detached pixels from an adjacent source-sheet cell. | Derive a clean overlay by deterministically trimming only the detached neighbor fragment while preserving the animation viewport. |
| `library:env-02-screen-themes:screen.theme.content.b` | `animated-shell` | `wall` | `derive-composite` | The overlay itself is coherent, but the processed crop contains detached pixels from an adjacent source-sheet cell. | Derive a clean overlay by deterministically trimming only the detached neighbor fragment while preserving the animation viewport. |
| `library:env-02-screen-themes:screen.theme.content.c` | `animated-shell` | `wall` | `derive-composite` | The overlay itself is coherent, but the processed crop contains detached pixels from an adjacent source-sheet cell. | Derive a clean overlay by deterministically trimming only the detached neighbor fragment while preserving the animation viewport. |
| `library:env-02-screen-themes:screen.theme.content.d` | `animated-shell` | `wall` | `derive-composite` | The overlay itself is coherent, but the processed crop contains detached pixels from an adjacent source-sheet cell. | Derive a clean overlay by deterministically trimming only the detached neighbor fragment while preserving the animation viewport. |
| `library:env-02-screen-themes:screen.theme.dashboard.a` | `animated-shell` | `wall` | `derive-composite` | The overlay itself is coherent, but the processed crop contains detached pixels from an adjacent source-sheet cell. | Derive a clean overlay by deterministically trimming only the detached neighbor fragment while preserving the animation viewport. |
| `library:env-02-screen-themes:screen.theme.dashboard.b` | `animated-shell` | `wall` | `derive-composite` | The overlay itself is coherent, but the processed crop contains detached pixels from an adjacent source-sheet cell. | Derive a clean overlay by deterministically trimming only the detached neighbor fragment while preserving the animation viewport. |
| `library:env-02-screen-themes:screen.theme.dashboard.c` | `animated-shell` | `wall` | `derive-composite` | The overlay itself is coherent, but the processed crop contains detached pixels from an adjacent source-sheet cell. | Derive a clean overlay by deterministically trimming only the detached neighbor fragment while preserving the animation viewport. |
| `library:env-02-screen-themes:screen.theme.dashboard.d` | `animated-shell` | `wall` | `derive-composite` | The overlay itself is coherent, but the processed crop contains detached pixels from an adjacent source-sheet cell. | Derive a clean overlay by deterministically trimming only the detached neighbor fragment while preserving the animation viewport. |
| `library:env-02-screen-themes:screen.theme.progress.a` | `animated-shell` | `wall` | `derive-composite` | The overlay itself is coherent, but the processed crop contains detached pixels from an adjacent source-sheet cell. | Derive a clean overlay by deterministically trimming only the detached neighbor fragment while preserving the animation viewport. |
| `library:env-02-screen-themes:screen.theme.progress.b` | `animated-shell` | `wall` | `derive-composite` | The overlay itself is coherent, but the processed crop contains detached pixels from an adjacent source-sheet cell. | Derive a clean overlay by deterministically trimming only the detached neighbor fragment while preserving the animation viewport. |
| `library:env-02-screen-themes:screen.theme.progress.c` | `animated-shell` | `wall` | `derive-composite` | The overlay itself is coherent, but the processed crop contains detached pixels from an adjacent source-sheet cell. | Derive a clean overlay by deterministically trimming only the detached neighbor fragment while preserving the animation viewport. |
| `library:env-02-screen-themes:screen.theme.progress.d` | `animated-shell` | `wall` | `derive-composite` | The overlay itself is coherent, but the processed crop contains detached pixels from an adjacent source-sheet cell. | Derive a clean overlay by deterministically trimming only the detached neighbor fragment while preserving the animation viewport. |
| `library:env-02-screen-themes:screen.theme.support.a` | `animated-shell` | `wall` | `derive-composite` | The overlay itself is coherent, but the processed crop contains detached pixels from an adjacent source-sheet cell. | Derive a clean overlay by deterministically trimming only the detached neighbor fragment while preserving the animation viewport. |
| `library:env-02-screen-themes:screen.theme.support.b` | `animated-shell` | `wall` | `derive-composite` | The overlay itself is coherent, but the processed crop contains detached pixels from an adjacent source-sheet cell. | Derive a clean overlay by deterministically trimming only the detached neighbor fragment while preserving the animation viewport. |
| `library:env-02-screen-themes:screen.theme.support.c` | `animated-shell` | `wall` | `derive-composite` | The overlay itself is coherent, but the processed crop contains detached pixels from an adjacent source-sheet cell. | Derive a clean overlay by deterministically trimming only the detached neighbor fragment while preserving the animation viewport. |
| `library:env-02-screen-themes:screen.theme.support.d` | `animated-shell` | `wall` | `derive-composite` | The overlay itself is coherent, but the processed crop contains detached pixels from an adjacent source-sheet cell. | Derive a clean overlay by deterministically trimming only the detached neighbor fragment while preserving the animation viewport. |
| `library:env-03-research-creative:backdrop.stand` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the source sheet: isolated props have clear physical bases or parent-surface contact and no baked furniture. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-03-research-creative:board.comparison` | `wall-mounted` | `wall` | `metadata-fix` | Reviewed the source sheet: isolated props have clear physical bases or parent-surface contact and no baked furniture. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-03-research-creative:camera.tripod` | `upright-floor-object` | `floor` | `derive-composite` | The main prop silhouette is usable, but its processed crop includes a detached sliver from a neighboring cell. | Create a deterministic clean derivative that removes the isolated sliver without repainting the accepted prop pixels. |
| `library:env-03-research-creative:dock.stylus` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: isolated props have clear physical bases or parent-surface contact and no baked furniture. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-03-research-creative:headphones.office` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: isolated props have clear physical bases or parent-surface contact and no baked furniture. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-03-research-creative:light.studio` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the source sheet: isolated props have clear physical bases or parent-surface contact and no baked furniture. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-03-research-creative:lightbox.product` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: isolated props have clear physical bases or parent-surface contact and no baked furniture. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-03-research-creative:microphone.desktop` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: isolated props have clear physical bases or parent-surface contact and no baked furniture. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-03-research-creative:package.mockup` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: isolated props have clear physical bases or parent-surface contact and no baked furniture. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-03-research-creative:parcel.sample` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: isolated props have clear physical bases or parent-surface contact and no baked furniture. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-03-research-creative:sample.tray.product` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: isolated props have clear physical bases or parent-surface contact and no baked furniture. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-03-research-creative:scale.digital` | `upright-floor-object` | `furniture-surface` | `derive-composite` | The main prop silhouette is usable, but its processed crop includes a detached sliver from a neighboring cell. | Create a deterministic clean derivative that removes the isolated sliver without repainting the accepted prop pixels. |
| `library:env-03-research-creative:scanner.barcode` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: isolated props have clear physical bases or parent-surface contact and no baked furniture. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-03-research-creative:stand.phone` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: isolated props have clear physical bases or parent-surface contact and no baked furniture. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-03-research-creative:swatches.color` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: isolated props have clear physical bases or parent-surface contact and no baked furniture. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-03-research-creative:turntable.display` | `upright-floor-object` | `furniture-surface` | `derive-composite` | The main prop silhouette is usable, but its processed crop includes a detached sliver from a neighboring cell. | Create a deterministic clean derivative that removes the isolated sliver without repainting the accepted prop pixels. |
| `library:env-04-release-noc:alert.beacon` | `upright-floor-object` | `furniture-surface` | `derive-composite` | Contact-sheet review found detached adjacent-cell pixels outside the intended equipment silhouette. | Derive a clean crop with the stray component removed; keep the original source extraction unchanged for provenance. |
| `library:env-04-release-noc:backup.drive.dock` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: NOC and QA props are isolated; tall equipment uses a readable one-cell-deep frontal shell. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-04-release-noc:cable.management.tray` | `upright-floor-object` | `furniture-surface` | `derive-composite` | Contact-sheet review found detached adjacent-cell pixels outside the intended equipment silhouette. | Derive a clean crop with the stray component removed; keep the original source extraction unchanged for provenance. |
| `library:env-04-release-noc:incident.response.bag` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: NOC and QA props are isolated; tall equipment uses a readable one-cell-deep frontal shell. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-04-release-noc:network.router.firewall` | `upright-floor-object` | `furniture-surface` | `derive-composite` | Contact-sheet review found detached adjacent-cell pixels outside the intended equipment silhouette. | Derive a clean crop with the stray component removed; keep the original source extraction unchanged for provenance. |
| `library:env-04-release-noc:network.switch` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: NOC and QA props are isolated; tall equipment uses a readable one-cell-deep frontal shell. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-04-release-noc:noc.monitoring.panel` | `wall-mounted` | `wall` | `metadata-fix` | Reviewed the source sheet: NOC and QA props are isolated; tall equipment uses a readable one-cell-deep frontal shell. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-04-release-noc:printer.barcode` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: NOC and QA props are isolated; tall equipment uses a readable one-cell-deep frontal shell. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-04-release-noc:qa.checklist` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: NOC and QA props are isolated; tall equipment uses a readable one-cell-deep frontal shell. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-04-release-noc:qa.handheld.controller` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: NOC and QA props are isolated; tall equipment uses a readable one-cell-deep frontal shell. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-04-release-noc:qa.status.board` | `wall-mounted` | `wall` | `metadata-fix` | Reviewed the source sheet: NOC and QA props are isolated; tall equipment uses a readable one-cell-deep frontal shell. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-04-release-noc:qa.test.device` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: NOC and QA props are isolated; tall equipment uses a readable one-cell-deep frontal shell. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-04-release-noc:qa.triage.terminal` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: NOC and QA props are isolated; tall equipment uses a readable one-cell-deep frontal shell. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-04-release-noc:release.laptop` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: NOC and QA props are isolated; tall equipment uses a readable one-cell-deep frontal shell. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-04-release-noc:release.package.crate` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the source sheet: NOC and QA props are isolated; tall equipment uses a readable one-cell-deep frontal shell. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-04-release-noc:server.rack.noc` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the source sheet: NOC and QA props are isolated; tall equipment uses a readable one-cell-deep frontal shell. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-05-facility-lounge:beanbag.lounge` | `seat` | `floor` | `metadata-fix` | Reviewed the source sheet: front shells and seat surfaces are legible, while furniture still needs explicit bases, slots, and occlusion semantics. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. The missing layer declaration must be corrected. |
| `library:env-05-facility-lounge:bookshelf.reading` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the source sheet: front shells and seat surfaces are legible, while furniture still needs explicit bases, slots, and occlusion semantics. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. The missing layer declaration must be corrected. |
| `library:env-05-facility-lounge:case.figure-display` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the source sheet: front shells and seat surfaces are legible, while furniture still needs explicit bases, slots, and occlusion semantics. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. The missing layer declaration must be corrected. |
| `library:env-05-facility-lounge:chair.massage.modern` | `seat` | `floor` | `metadata-fix` | Reviewed the source sheet: front shells and seat surfaces are legible, while furniture still needs explicit bases, slots, and occlusion semantics. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. The missing layer declaration must be corrected. |
| `library:env-05-facility-lounge:chair.reading` | `seat` | `floor` | `metadata-fix` | Reviewed the source sheet: front shells and seat surfaces are legible, while furniture still needs explicit bases, slots, and occlusion semantics. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. The missing layer declaration must be corrected. |
| `library:env-05-facility-lounge:facility.status.a` | `animated-shell` | `wall` | `reuse` | Reviewed the source sheet: front shells and seat surfaces are legible, while furniture still needs explicit bases, slots, and occlusion semantics. | Reuse the pixel content and stable animation bounds; map placement and pivots through Geometry v3 without editing the image. |
| `library:env-05-facility-lounge:facility.status.b` | `animated-shell` | `wall` | `reuse` | Reviewed the source sheet: front shells and seat surfaces are legible, while furniture still needs explicit bases, slots, and occlusion semantics. | Reuse the pixel content and stable animation bounds; map placement and pivots through Geometry v3 without editing the image. |
| `library:env-05-facility-lounge:facility.status.c` | `animated-shell` | `wall` | `reuse` | Reviewed the source sheet: front shells and seat surfaces are legible, while furniture still needs explicit bases, slots, and occlusion semantics. | Reuse the pixel content and stable animation bounds; map placement and pivots through Geometry v3 without editing the image. |
| `library:env-05-facility-lounge:facility.status.d` | `animated-shell` | `wall` | `reuse` | Reviewed the source sheet: front shells and seat surfaces are legible, while furniture still needs explicit bases, slots, and occlusion semantics. | Reuse the pixel content and stable animation bounds; map placement and pivots through Geometry v3 without editing the image. |
| `library:env-05-facility-lounge:locker.bank.personal-15` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the source sheet: front shells and seat surfaces are legible, while furniture still needs explicit bases, slots, and occlusion semantics. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. The missing layer declaration must be corrected. |
| `library:env-05-facility-lounge:machine.game.arcade.modern` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the source sheet: front shells and seat surfaces are legible, while furniture still needs explicit bases, slots, and occlusion semantics. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. The missing layer declaration must be corrected. |
| `library:env-05-facility-lounge:refrigerator.modern` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the source sheet: front shells and seat surfaces are legible, while furniture still needs explicit bases, slots, and occlusion semantics. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. The missing layer declaration must be corrected. |
| `library:env-05-facility-lounge:sofa.modern.three-seat` | `seat` | `floor` | `derive-composite` | Reviewed the source sheet: front shells and seat surfaces are legible, while furniture still needs explicit bases, slots, and occlusion semantics. | Reuse the accepted pixels where possible and derive deterministic support, underframe/base, and foreground occlusion parts. |
| `library:env-05-facility-lounge:sofa.modern.two-seat` | `seat` | `floor` | `derive-composite` | Reviewed the source sheet: front shells and seat surfaces are legible, while furniture still needs explicit bases, slots, and occlusion semantics. | Reuse the accepted pixels where possible and derive deterministic support, underframe/base, and foreground occlusion parts. |
| `library:env-05-facility-lounge:table.board-game` | `surface-furniture` | `floor` | `derive-composite` | Reviewed the source sheet: front shells and seat surfaces are legible, while furniture still needs explicit bases, slots, and occlusion semantics. | Reuse the accepted pixels where possible and derive deterministic support, underframe/base, and foreground occlusion parts. |
| `library:env-05-facility-lounge:vending.machine.modern` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the source sheet: front shells and seat surfaces are legible, while furniture still needs explicit bases, slots, and occlusion semantics. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. The missing layer declaration must be corrected. |
| `library:env-06-decor-architecture-tv:art.abstract.frame` | `wall-mounted` | `wall` | `metadata-fix` | Reviewed the source sheet: wall art and upright decor are reusable; rugs and furniture require corrected semantic planes. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-06-decor-architecture-tv:clock.wall` | `wall-mounted` | `wall` | `metadata-fix` | Reviewed the source sheet: wall art and upright decor are reusable; rugs and furniture require corrected semantic planes. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-06-decor-architecture-tv:divider.planter` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the source sheet: wall art and upright decor are reusable; rugs and furniture require corrected semantic planes. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-06-decor-architecture-tv:lamp.floor` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the source sheet: wall art and upright decor are reusable; rugs and furniture require corrected semantic planes. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-06-decor-architecture-tv:panel.acoustic` | `wall-mounted` | `wall` | `metadata-fix` | Reviewed the source sheet: wall art and upright decor are reusable; rugs and furniture require corrected semantic planes. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-06-decor-architecture-tv:plant.tall` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the source sheet: wall art and upright decor are reusable; rugs and furniture require corrected semantic planes. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-06-decor-architecture-tv:pouf.lounge` | `seat` | `floor` | `metadata-fix` | Reviewed the source sheet: wall art and upright decor are reusable; rugs and furniture require corrected semantic planes. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-06-decor-architecture-tv:rack.coat` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the source sheet: wall art and upright decor are reusable; rugs and furniture require corrected semantic planes. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-06-decor-architecture-tv:rug.office` | `floor-decal` | `floor` | `metadata-fix` | Reviewed the source sheet: wall art and upright decor are reusable; rugs and furniture require corrected semantic planes. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-06-decor-architecture-tv:shelf.wall` | `wall-mounted` | `wall` | `metadata-fix` | Reviewed the source sheet: wall art and upright decor are reusable; rugs and furniture require corrected semantic planes. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-06-decor-architecture-tv:stand.umbrella` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the source sheet: wall art and upright decor are reusable; rugs and furniture require corrected semantic planes. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-06-decor-architecture-tv:table.side` | `surface-furniture` | `floor` | `derive-composite` | Reviewed the source sheet: wall art and upright decor are reusable; rugs and furniture require corrected semantic planes. | Reuse the accepted pixels where possible and derive deterministic support, underframe/base, and foreground occlusion parts. |
| `library:env-06-decor-architecture-tv:tv.wall.a` | `animated-shell` | `wall` | `reuse` | Reviewed the source sheet: wall art and upright decor are reusable; rugs and furniture require corrected semantic planes. | Reuse the pixel content and stable animation bounds; map placement and pivots through Geometry v3 without editing the image. |
| `library:env-06-decor-architecture-tv:tv.wall.b` | `animated-shell` | `wall` | `reuse` | Reviewed the source sheet: wall art and upright decor are reusable; rugs and furniture require corrected semantic planes. | Reuse the pixel content and stable animation bounds; map placement and pivots through Geometry v3 without editing the image. |
| `library:env-06-decor-architecture-tv:tv.wall.c` | `animated-shell` | `wall` | `reuse` | Reviewed the source sheet: wall art and upright decor are reusable; rugs and furniture require corrected semantic planes. | Reuse the pixel content and stable animation bounds; map placement and pivots through Geometry v3 without editing the image. |
| `library:env-06-decor-architecture-tv:tv.wall.d` | `animated-shell` | `wall` | `reuse` | Reviewed the source sheet: wall art and upright decor are reusable; rugs and furniture require corrected semantic planes. | Reuse the pixel content and stable animation bounds; map placement and pivots through Geometry v3 without editing the image. |
| `library:env-07-animated-mechanical:machine.arcade.loop.a` | `animated-shell` | `floor` | `derive-composite` | The four-frame loop is coherent, but each processed frame carries detached row-overflow pixels above or below the shell. | Trim the same detached component from every frame and preserve identical loop bounds and timing. |
| `library:env-07-animated-mechanical:machine.arcade.loop.b` | `animated-shell` | `floor` | `derive-composite` | The four-frame loop is coherent, but each processed frame carries detached row-overflow pixels above or below the shell. | Trim the same detached component from every frame and preserve identical loop bounds and timing. |
| `library:env-07-animated-mechanical:machine.arcade.loop.c` | `animated-shell` | `floor` | `derive-composite` | The four-frame loop is coherent, but each processed frame carries detached row-overflow pixels above or below the shell. | Trim the same detached component from every frame and preserve identical loop bounds and timing. |
| `library:env-07-animated-mechanical:machine.arcade.loop.d` | `animated-shell` | `floor` | `derive-composite` | The four-frame loop is coherent, but each processed frame carries detached row-overflow pixels above or below the shell. | Trim the same detached component from every frame and preserve identical loop bounds and timing. |
| `library:env-07-animated-mechanical:printer.label.loop.a` | `animated-shell` | `furniture-surface` | `derive-composite` | The four-frame loop is coherent, but each processed frame carries detached row-overflow pixels above or below the shell. | Trim the same detached component from every frame and preserve identical loop bounds and timing. |
| `library:env-07-animated-mechanical:printer.label.loop.b` | `animated-shell` | `furniture-surface` | `derive-composite` | The four-frame loop is coherent, but each processed frame carries detached row-overflow pixels above or below the shell. | Trim the same detached component from every frame and preserve identical loop bounds and timing. |
| `library:env-07-animated-mechanical:printer.label.loop.c` | `animated-shell` | `furniture-surface` | `derive-composite` | The four-frame loop is coherent, but each processed frame carries detached row-overflow pixels above or below the shell. | Trim the same detached component from every frame and preserve identical loop bounds and timing. |
| `library:env-07-animated-mechanical:printer.label.loop.d` | `animated-shell` | `furniture-surface` | `derive-composite` | The four-frame loop is coherent, but each processed frame carries detached row-overflow pixels above or below the shell. | Trim the same detached component from every frame and preserve identical loop bounds and timing. |
| `library:env-07-animated-mechanical:server.rack.loop.a` | `animated-shell` | `floor` | `reuse` | Reviewed all four-frame rows: shell silhouettes and bases remain stable while only declared local regions change. | Reuse the pixel content and stable animation bounds; map placement and pivots through Geometry v3 without editing the image. |
| `library:env-07-animated-mechanical:server.rack.loop.b` | `animated-shell` | `floor` | `reuse` | Reviewed all four-frame rows: shell silhouettes and bases remain stable while only declared local regions change. | Reuse the pixel content and stable animation bounds; map placement and pivots through Geometry v3 without editing the image. |
| `library:env-07-animated-mechanical:server.rack.loop.c` | `animated-shell` | `floor` | `reuse` | Reviewed all four-frame rows: shell silhouettes and bases remain stable while only declared local regions change. | Reuse the pixel content and stable animation bounds; map placement and pivots through Geometry v3 without editing the image. |
| `library:env-07-animated-mechanical:server.rack.loop.d` | `animated-shell` | `floor` | `reuse` | Reviewed all four-frame rows: shell silhouettes and bases remain stable while only declared local regions change. | Reuse the pixel content and stable animation bounds; map placement and pivots through Geometry v3 without editing the image. |
| `library:env-07-animated-mechanical:vending.machine.loop.a` | `animated-shell` | `floor` | `derive-composite` | The four-frame loop is coherent, but each processed frame carries detached row-overflow pixels above or below the shell. | Trim the same detached component from every frame and preserve identical loop bounds and timing. |
| `library:env-07-animated-mechanical:vending.machine.loop.b` | `animated-shell` | `floor` | `derive-composite` | The four-frame loop is coherent, but each processed frame carries detached row-overflow pixels above or below the shell. | Trim the same detached component from every frame and preserve identical loop bounds and timing. |
| `library:env-07-animated-mechanical:vending.machine.loop.c` | `animated-shell` | `floor` | `derive-composite` | The four-frame loop is coherent, but each processed frame carries detached row-overflow pixels above or below the shell. | Trim the same detached component from every frame and preserve identical loop bounds and timing. |
| `library:env-07-animated-mechanical:vending.machine.loop.d` | `animated-shell` | `floor` | `derive-composite` | The four-frame loop is coherent, but each processed frame carries detached row-overflow pixels above or below the shell. | Trim the same detached component from every frame and preserve identical loop bounds and timing. |
| `library:env-08-animated-ambient:dispenser.water.loop.a` | `animated-shell` | `floor` | `reuse` | Reviewed all four-frame rows: base contact remains stable across water, coffee, plant, and lamp loops. | Reuse the pixel content and stable animation bounds; map placement and pivots through Geometry v3 without editing the image. |
| `library:env-08-animated-ambient:dispenser.water.loop.b` | `animated-shell` | `floor` | `reuse` | Reviewed all four-frame rows: base contact remains stable across water, coffee, plant, and lamp loops. | Reuse the pixel content and stable animation bounds; map placement and pivots through Geometry v3 without editing the image. |
| `library:env-08-animated-ambient:dispenser.water.loop.c` | `animated-shell` | `floor` | `reuse` | Reviewed all four-frame rows: base contact remains stable across water, coffee, plant, and lamp loops. | Reuse the pixel content and stable animation bounds; map placement and pivots through Geometry v3 without editing the image. |
| `library:env-08-animated-ambient:dispenser.water.loop.d` | `animated-shell` | `floor` | `reuse` | Reviewed all four-frame rows: base contact remains stable across water, coffee, plant, and lamp loops. | Reuse the pixel content and stable animation bounds; map placement and pivots through Geometry v3 without editing the image. |
| `library:env-08-animated-ambient:lamp.desk.loop.a` | `animated-shell` | `furniture-surface` | `derive-composite` | The ambient animation is usable, but source-row overflow appears as detached fragments outside the intended frame silhouette. | Produce a frame-stable derivative that removes only the detached overflow pixels from all four frames. |
| `library:env-08-animated-ambient:lamp.desk.loop.b` | `animated-shell` | `furniture-surface` | `derive-composite` | The ambient animation is usable, but source-row overflow appears as detached fragments outside the intended frame silhouette. | Produce a frame-stable derivative that removes only the detached overflow pixels from all four frames. |
| `library:env-08-animated-ambient:lamp.desk.loop.c` | `animated-shell` | `furniture-surface` | `derive-composite` | The ambient animation is usable, but source-row overflow appears as detached fragments outside the intended frame silhouette. | Produce a frame-stable derivative that removes only the detached overflow pixels from all four frames. |
| `library:env-08-animated-ambient:lamp.desk.loop.d` | `animated-shell` | `furniture-surface` | `derive-composite` | The ambient animation is usable, but source-row overflow appears as detached fragments outside the intended frame silhouette. | Produce a frame-stable derivative that removes only the detached overflow pixels from all four frames. |
| `library:env-08-animated-ambient:machine.coffee.loop.a` | `animated-shell` | `furniture-surface` | `derive-composite` | The ambient animation is usable, but source-row overflow appears as detached fragments outside the intended frame silhouette. | Produce a frame-stable derivative that removes only the detached overflow pixels from all four frames. |
| `library:env-08-animated-ambient:machine.coffee.loop.b` | `animated-shell` | `furniture-surface` | `derive-composite` | The ambient animation is usable, but source-row overflow appears as detached fragments outside the intended frame silhouette. | Produce a frame-stable derivative that removes only the detached overflow pixels from all four frames. |
| `library:env-08-animated-ambient:machine.coffee.loop.c` | `animated-shell` | `furniture-surface` | `derive-composite` | The ambient animation is usable, but source-row overflow appears as detached fragments outside the intended frame silhouette. | Produce a frame-stable derivative that removes only the detached overflow pixels from all four frames. |
| `library:env-08-animated-ambient:machine.coffee.loop.d` | `animated-shell` | `furniture-surface` | `derive-composite` | The ambient animation is usable, but source-row overflow appears as detached fragments outside the intended frame silhouette. | Produce a frame-stable derivative that removes only the detached overflow pixels from all four frames. |
| `library:env-08-animated-ambient:plant.small.loop.a` | `animated-shell` | `floor` | `derive-composite` | The ambient animation is usable, but source-row overflow appears as detached fragments outside the intended frame silhouette. | Produce a frame-stable derivative that removes only the detached overflow pixels from all four frames. |
| `library:env-08-animated-ambient:plant.small.loop.b` | `animated-shell` | `floor` | `derive-composite` | The ambient animation is usable, but source-row overflow appears as detached fragments outside the intended frame silhouette. | Produce a frame-stable derivative that removes only the detached overflow pixels from all four frames. |
| `library:env-08-animated-ambient:plant.small.loop.c` | `animated-shell` | `floor` | `derive-composite` | The ambient animation is usable, but source-row overflow appears as detached fragments outside the intended frame silhouette. | Produce a frame-stable derivative that removes only the detached overflow pixels from all four frames. |
| `library:env-08-animated-ambient:plant.small.loop.d` | `animated-shell` | `floor` | `derive-composite` | The ambient animation is usable, but source-row overflow appears as detached fragments outside the intended frame silhouette. | Produce a frame-stable derivative that removes only the detached overflow pixels from all four frames. |
| `library:env-09-phase2-completion-architecture:cable.cover.floor` | `floor-decal` | `floor` | `metadata-fix` | Reviewed the source sheet: side desks still omit top-down depth; monitor profiles and architecture objects remain isolated. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-09-phase2-completion-architecture:desk.workstation.left` | `surface-furniture` | `floor` | `regenerate` | Reviewed the source sheet: side desks still omit top-down depth; monitor profiles and architecture objects remain isolated. | Regenerate after Step 4 because the visible bitmap cannot represent the approved 5 x 4 footprint and 5 x 3 support plane. |
| `library:env-09-phase2-completion-architecture:desk.workstation.right` | `surface-furniture` | `floor` | `regenerate` | Reviewed the source sheet: side desks still omit top-down depth; monitor profiles and architecture objects remain isolated. | Regenerate after Step 4 because the visible bitmap cannot represent the approved 5 x 4 footprint and 5 x 3 support plane. |
| `library:env-09-phase2-completion-architecture:emergency.light.wall` | `wall-mounted` | `wall` | `derive-composite` | The intended architecture prop can be isolated, but the processed bitmap includes a detached neighbor fragment. | Create a clean derivative by selecting the intended connected silhouette; retain the unedited extraction as source evidence. |
| `library:env-09-phase2-completion-architecture:light.wall.decorative` | `wall-mounted` | `wall` | `derive-composite` | The intended architecture prop can be isolated, but the processed bitmap includes a detached neighbor fragment. | Create a clean derivative by selecting the intended connected silhouette; retain the unedited extraction as source evidence. |
| `library:env-09-phase2-completion-architecture:monitor.shell.left` | `upright-floor-object` | `furniture-surface` | `derive-composite` | The intended architecture prop can be isolated, but the processed bitmap includes a detached neighbor fragment. | Create a clean derivative by selecting the intended connected silhouette; retain the unedited extraction as source evidence. |
| `library:env-09-phase2-completion-architecture:monitor.shell.right` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: side desks still omit top-down depth; monitor profiles and architecture objects remain isolated. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-09-phase2-completion-architecture:ornament.small` | `upright-floor-object` | `furniture-surface` | `derive-composite` | The intended architecture prop can be isolated, but the processed bitmap includes a detached neighbor fragment. | Create a clean derivative by selecting the intended connected silhouette; retain the unedited extraction as source evidence. |
| `library:env-09-phase2-completion-architecture:partition.glass` | `upright-floor-object` | `floor` | `derive-composite` | The intended architecture prop can be isolated, but the processed bitmap includes a detached neighbor fragment. | Create a clean derivative by selecting the intended connected silhouette; retain the unedited extraction as source evidence. |
| `library:env-09-phase2-completion-architecture:pinboard.team` | `wall-mounted` | `wall` | `metadata-fix` | Reviewed the source sheet: side desks still omit top-down depth; monitor profiles and architecture objects remain isolated. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-09-phase2-completion-architecture:screen.theme.system.a` | `animated-shell` | `wall` | `reuse` | Reviewed the source sheet: side desks still omit top-down depth; monitor profiles and architecture objects remain isolated. | Reuse the pixel content and stable animation bounds; map placement and pivots through Geometry v3 without editing the image. |
| `library:env-09-phase2-completion-architecture:screen.theme.system.b` | `animated-shell` | `wall` | `reuse` | Reviewed the source sheet: side desks still omit top-down depth; monitor profiles and architecture objects remain isolated. | Reuse the pixel content and stable animation bounds; map placement and pivots through Geometry v3 without editing the image. |
| `library:env-09-phase2-completion-architecture:screen.theme.system.c` | `animated-shell` | `wall` | `reuse` | Reviewed the source sheet: side desks still omit top-down depth; monitor profiles and architecture objects remain isolated. | Reuse the pixel content and stable animation bounds; map placement and pivots through Geometry v3 without editing the image. |
| `library:env-09-phase2-completion-architecture:screen.theme.system.d` | `animated-shell` | `wall` | `reuse` | Reviewed the source sheet: side desks still omit top-down depth; monitor profiles and architecture objects remain isolated. | Reuse the pixel content and stable animation bounds; map placement and pivots through Geometry v3 without editing the image. |
| `library:env-09-phase2-completion-architecture:sign.exit.modern` | `wall-mounted` | `wall` | `derive-composite` | The intended architecture prop can be isolated, but the processed bitmap includes a detached neighbor fragment. | Create a clean derivative by selecting the intended connected silhouette; retain the unedited extraction as source evidence. |
| `library:env-09-phase2-completion-architecture:whiteboard.mobile` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the source sheet: side desks still omit top-down depth; monitor profiles and architecture objects remain isolated. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-10-storage-operations-detail:bin.paper.modern` | `upright-floor-object` | `floor` | `derive-composite` | The storage prop is usable, but the crop includes detached pixels overflowing from the source-sheet row above. | Derive a clean asset by removing the isolated overflow component without altering the accepted main silhouette. |
| `library:env-10-storage-operations-detail:bin.recycling.modern` | `upright-floor-object` | `floor` | `derive-composite` | The storage prop is usable, but the crop includes detached pixels overflowing from the source-sheet row above. | Derive a clean asset by removing the isolated overflow component without altering the accepted main silhouette. |
| `library:env-10-storage-operations-detail:bin.waste.modern` | `upright-floor-object` | `floor` | `derive-composite` | The storage prop is usable, but the crop includes detached pixels overflowing from the source-sheet row above. | Derive a clean asset by removing the isolated overflow component without altering the accepted main silhouette. |
| `library:env-10-storage-operations-detail:cabinet.storage.low` | `surface-furniture` | `floor` | `derive-composite` | Reviewed the source sheet: tall shells are reusable; low storage and carts need explicit support/composite semantics. | Reuse the accepted pixels where possible and derive deterministic support, underframe/base, and foreground occlusion parts. |
| `library:env-10-storage-operations-detail:cabinet.storage.tall` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the source sheet: tall shells are reusable; low storage and carts need explicit support/composite semantics. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-10-storage-operations-detail:cart.utility` | `surface-furniture` | `floor` | `derive-composite` | Reviewed the source sheet: tall shells are reusable; low storage and carts need explicit support/composite semantics. | Reuse the accepted pixels where possible and derive deterministic support, underframe/base, and foreground occlusion parts. |
| `library:env-10-storage-operations-detail:cctv.camera.wall` | `wall-mounted` | `wall` | `metadata-fix` | Reviewed the source sheet: tall shells are reusable; low storage and carts need explicit support/composite semantics. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-10-storage-operations-detail:clock.digital` | `wall-mounted` | `wall` | `metadata-fix` | Reviewed the source sheet: tall shells are reusable; low storage and carts need explicit support/composite semantics. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-10-storage-operations-detail:document.tray` | `upright-floor-object` | `furniture-surface` | `derive-composite` | The storage prop is usable, but the crop includes detached pixels overflowing from the source-sheet row above. | Derive a clean asset by removing the isolated overflow component without altering the accepted main silhouette. |
| `library:env-10-storage-operations-detail:drawer.archive` | `upright-floor-object` | `floor` | `derive-composite` | The storage prop is usable, but the crop includes detached pixels overflowing from the source-sheet row above. | Derive a clean asset by removing the isolated overflow component without altering the accepted main silhouette. |
| `library:env-10-storage-operations-detail:extinguisher.wall.modern` | `wall-mounted` | `wall` | `metadata-fix` | Reviewed the source sheet: tall shells are reusable; low storage and carts need explicit support/composite semantics. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-10-storage-operations-detail:first.aid.wall` | `wall-mounted` | `wall` | `derive-composite` | The storage prop is usable, but the crop includes detached pixels overflowing from the source-sheet row above. | Derive a clean asset by removing the isolated overflow component without altering the accepted main silhouette. |
| `library:env-10-storage-operations-detail:label.box` | `upright-floor-object` | `furniture-surface` | `derive-composite` | The storage prop is usable, but the crop includes detached pixels overflowing from the source-sheet row above. | Derive a clean asset by removing the isolated overflow component without altering the accepted main silhouette. |
| `library:env-10-storage-operations-detail:mail.sorter` | `upright-floor-object` | `furniture-surface` | `derive-composite` | The storage prop is usable, but the crop includes detached pixels overflowing from the source-sheet row above. | Derive a clean asset by removing the isolated overflow component without altering the accepted main silhouette. |
| `library:env-10-storage-operations-detail:shelf.storage.tall` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the source sheet: tall shells are reusable; low storage and carts need explicit support/composite semantics. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-10-storage-operations-detail:smoke.detector.wall` | `wall-mounted` | `wall` | `metadata-fix` | Reviewed the source sheet: tall shells are reusable; low storage and carts need explicit support/composite semantics. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-11-comfort-personal-detail:bag.hook.wall` | `wall-mounted` | `wall` | `metadata-fix` | Reviewed the source sheet: small props have clear floor, wall, or furniture-surface bases and usable alpha separation. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-11-comfort-personal-detail:cable.grommet` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: small props have clear floor, wall, or furniture-surface bases and usable alpha separation. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-11-comfort-personal-detail:coat.hooks.wall` | `wall-mounted` | `wall` | `derive-composite` | The intended personal-detail prop is intact, with a small detached neighbor fragment elsewhere in the processed crop. | Create a deterministic trimmed derivative and preserve the original extraction for audit provenance. |
| `library:env-11-comfort-personal-detail:cushion.lounge` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: small props have clear floor, wall, or furniture-surface bases and usable alpha separation. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-11-comfort-personal-detail:desk.nameplate.blank` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: small props have clear floor, wall, or furniture-surface bases and usable alpha separation. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-11-comfort-personal-detail:footrest.office` | `upright-floor-object` | `floor` | `derive-composite` | The intended personal-detail prop is intact, with a small detached neighbor fragment elsewhere in the processed crop. | Create a deterministic trimmed derivative and preserve the original extraction for audit provenance. |
| `library:env-11-comfort-personal-detail:headphone.hook` | `wall-mounted` | `wall` | `metadata-fix` | Reviewed the source sheet: small props have clear floor, wall, or furniture-surface bases and usable alpha separation. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-11-comfort-personal-detail:monitor.arm` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: small props have clear floor, wall, or furniture-surface bases and usable alpha separation. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-11-comfort-personal-detail:mug.stack` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: small props have clear floor, wall, or furniture-surface bases and usable alpha separation. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-11-comfort-personal-detail:pen.stand` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: small props have clear floor, wall, or furniture-surface bases and usable alpha separation. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-11-comfort-personal-detail:plant.corner.large` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the source sheet: small props have clear floor, wall, or furniture-surface bases and usable alpha separation. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-11-comfort-personal-detail:plant.hanging` | `wall-mounted` | `wall` | `metadata-fix` | Reviewed the source sheet: small props have clear floor, wall, or furniture-surface bases and usable alpha separation. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-11-comfort-personal-detail:planter.round` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: small props have clear floor, wall, or furniture-surface bases and usable alpha separation. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-11-comfort-personal-detail:stationery.cup` | `upright-floor-object` | `furniture-surface` | `derive-composite` | The intended personal-detail prop is intact, with a small detached neighbor fragment elsewhere in the processed crop. | Create a deterministic trimmed derivative and preserve the original extraction for audit provenance. |
| `library:env-11-comfort-personal-detail:stool.side` | `seat` | `floor` | `derive-composite` | The intended personal-detail prop is intact, with a small detached neighbor fragment elsewhere in the processed crop. | Create a deterministic trimmed derivative and preserve the original extraction for audit provenance. |
| `library:env-11-comfort-personal-detail:water.cup.stack` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the source sheet: small props have clear floor, wall, or furniture-surface bases and usable alpha separation. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `library:env-12-facility-side-orientations:chair.massage.modern.side-left` | `seat` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: silhouettes, rendering style, and shell details do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-12-facility-side-orientations:chair.massage.modern.side-right` | `seat` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: silhouettes, rendering style, and shell details do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-12-facility-side-orientations:dispenser.water.side-left` | `upright-floor-object` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: silhouettes, rendering style, and shell details do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-12-facility-side-orientations:dispenser.water.side-right` | `upright-floor-object` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: silhouettes, rendering style, and shell details do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-12-facility-side-orientations:machine.coffee.side-left` | `upright-floor-object` | `furniture-surface` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: silhouettes, rendering style, and shell details do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-12-facility-side-orientations:machine.coffee.side-right` | `upright-floor-object` | `furniture-surface` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: silhouettes, rendering style, and shell details do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-12-facility-side-orientations:machine.game.arcade.modern.side-left` | `upright-floor-object` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: silhouettes, rendering style, and shell details do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-12-facility-side-orientations:machine.game.arcade.modern.side-right` | `upright-floor-object` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: silhouettes, rendering style, and shell details do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-12-facility-side-orientations:printer.desktop.side-left` | `upright-floor-object` | `furniture-surface` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: silhouettes, rendering style, and shell details do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-12-facility-side-orientations:printer.desktop.side-right` | `upright-floor-object` | `furniture-surface` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: silhouettes, rendering style, and shell details do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-12-facility-side-orientations:refrigerator.modern.side-left` | `upright-floor-object` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: silhouettes, rendering style, and shell details do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-12-facility-side-orientations:refrigerator.modern.side-right` | `upright-floor-object` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: silhouettes, rendering style, and shell details do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-12-facility-side-orientations:server.rack.noc.side-left` | `upright-floor-object` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: silhouettes, rendering style, and shell details do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-12-facility-side-orientations:server.rack.noc.side-right` | `upright-floor-object` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: silhouettes, rendering style, and shell details do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-12-facility-side-orientations:vending.machine.modern.side-left` | `upright-floor-object` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: silhouettes, rendering style, and shell details do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-12-facility-side-orientations:vending.machine.modern.side-right` | `upright-floor-object` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: silhouettes, rendering style, and shell details do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-13-lounge-storage-side-orientations:cabinet.storage.low.side-left` | `upright-floor-object` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: proportions and material treatment do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-13-lounge-storage-side-orientations:cabinet.storage.low.side-right` | `upright-floor-object` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: proportions and material treatment do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-13-lounge-storage-side-orientations:cabinet.storage.tall.side-left` | `upright-floor-object` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: proportions and material treatment do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-13-lounge-storage-side-orientations:cabinet.storage.tall.side-right` | `upright-floor-object` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: proportions and material treatment do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-13-lounge-storage-side-orientations:cart.utility.side-left` | `upright-floor-object` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: proportions and material treatment do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-13-lounge-storage-side-orientations:cart.utility.side-right` | `upright-floor-object` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: proportions and material treatment do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-13-lounge-storage-side-orientations:partition.glass.side-left` | `upright-floor-object` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: proportions and material treatment do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-13-lounge-storage-side-orientations:partition.glass.side-right` | `upright-floor-object` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: proportions and material treatment do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-13-lounge-storage-side-orientations:shelf.storage.tall.side-left` | `upright-floor-object` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: proportions and material treatment do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-13-lounge-storage-side-orientations:shelf.storage.tall.side-right` | `upright-floor-object` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: proportions and material treatment do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-13-lounge-storage-side-orientations:sofa.modern.three-seat.side-left` | `seat` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: proportions and material treatment do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-13-lounge-storage-side-orientations:sofa.modern.three-seat.side-right` | `seat` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: proportions and material treatment do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-13-lounge-storage-side-orientations:sofa.modern.two-seat.side-left` | `seat` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: proportions and material treatment do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-13-lounge-storage-side-orientations:sofa.modern.two-seat.side-right` | `seat` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: proportions and material treatment do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-13-lounge-storage-side-orientations:table.board-game.side-left` | `surface-furniture` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: proportions and material treatment do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |
| `library:env-13-lounge-storage-side-orientations:table.board-game.side-right` | `surface-furniture` | `floor` | `blocked-by-orientation` | Reviewed the side sheet against the accepted fronts: proportions and material treatment do not form faithful turnarounds. | Do not promote this orientation; replace it with a camera-locked view of the accepted front family. |

## Planned records

| Record | Type | Plane | Disposition | Visual finding | Reason |
| --- | --- | --- | --- | --- | --- |
| `planned:beanbag.lounge` | `seat` | `floor` | `metadata-fix` | No separate planned bitmap exists; the exact asset ID is present in the visually reviewed modern library. | Retire or link this stale planning record to library:env-05-facility-lounge:beanbag.lounge; do not generate a duplicate asset. |
| `planned:bookshelf.reading` | `upright-floor-object` | `floor` | `metadata-fix` | No separate planned bitmap exists; the exact asset ID is present in the visually reviewed modern library. | Retire or link this stale planning record to library:env-05-facility-lounge:bookshelf.reading; do not generate a duplicate asset. |
| `planned:case.figure-display` | `upright-floor-object` | `floor` | `metadata-fix` | No separate planned bitmap exists; the exact asset ID is present in the visually reviewed modern library. | Retire or link this stale planning record to library:env-05-facility-lounge:case.figure-display; do not generate a duplicate asset. |
| `planned:chair.massage.modern` | `seat` | `floor` | `metadata-fix` | No separate planned bitmap exists; the exact asset ID is present in the visually reviewed modern library. | Retire or link this stale planning record to library:env-05-facility-lounge:chair.massage.modern; do not generate a duplicate asset. |
| `planned:chair.reading` | `seat` | `floor` | `metadata-fix` | No separate planned bitmap exists; the exact asset ID is present in the visually reviewed modern library. | Retire or link this stale planning record to library:env-05-facility-lounge:chair.reading; do not generate a duplicate asset. |
| `planned:light.wall.decorative` | `wall-mounted` | `wall` | `metadata-fix` | No separate planned bitmap exists; the exact asset ID is present in the visually reviewed modern library. | Retire or link this stale planning record to library:env-09-phase2-completion-architecture:light.wall.decorative; do not generate a duplicate asset. |
| `planned:locker.bank.personal-15` | `upright-floor-object` | `floor` | `metadata-fix` | No separate planned bitmap exists; the exact asset ID is present in the visually reviewed modern library. | Retire or link this stale planning record to library:env-05-facility-lounge:locker.bank.personal-15; do not generate a duplicate asset. |
| `planned:machine.game.arcade.modern` | `upright-floor-object` | `floor` | `metadata-fix` | No separate planned bitmap exists; the exact asset ID is present in the visually reviewed modern library. | Retire or link this stale planning record to library:env-05-facility-lounge:machine.game.arcade.modern; do not generate a duplicate asset. |
| `planned:ornament.small` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | No separate planned bitmap exists; the exact asset ID is present in the visually reviewed modern library. | Retire or link this stale planning record to library:env-09-phase2-completion-architecture:ornament.small; do not generate a duplicate asset. |
| `planned:partition.glass` | `upright-floor-object` | `floor` | `metadata-fix` | No separate planned bitmap exists; the exact asset ID is present in the visually reviewed modern library. | Retire or link this stale planning record to library:env-09-phase2-completion-architecture:partition.glass; do not generate a duplicate asset. |
| `planned:refrigerator.modern` | `upright-floor-object` | `floor` | `metadata-fix` | No separate planned bitmap exists; the exact asset ID is present in the visually reviewed modern library. | Retire or link this stale planning record to library:env-05-facility-lounge:refrigerator.modern; do not generate a duplicate asset. |
| `planned:sofa.modern.three-seat` | `seat` | `floor` | `metadata-fix` | No separate planned bitmap exists; the exact asset ID is present in the visually reviewed modern library. | Retire or link this stale planning record to library:env-05-facility-lounge:sofa.modern.three-seat; do not generate a duplicate asset. |
| `planned:sofa.modern.two-seat` | `seat` | `floor` | `metadata-fix` | No separate planned bitmap exists; the exact asset ID is present in the visually reviewed modern library. | Retire or link this stale planning record to library:env-05-facility-lounge:sofa.modern.two-seat; do not generate a duplicate asset. |
| `planned:speaker.desktop` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | No separate planned bitmap exists; the exact asset ID is present in the visually reviewed modern library. | Retire or link this stale planning record to library:env-01-workstation-static:speaker.desktop; do not generate a duplicate asset. |
| `planned:table.board-game` | `surface-furniture` | `floor` | `metadata-fix` | No separate planned bitmap exists; the exact asset ID is present in the visually reviewed modern library. | Retire or link this stale planning record to library:env-05-facility-lounge:table.board-game; do not generate a duplicate asset. |
| `planned:vending.machine.modern` | `upright-floor-object` | `floor` | `metadata-fix` | No separate planned bitmap exists; the exact asset ID is present in the visually reviewed modern library. | Retire or link this stale planning record to library:env-05-facility-lounge:vending.machine.modern; do not generate a duplicate asset. |

## Runtime records

| Record | Type | Plane | Disposition | Visual finding | Reason |
| --- | --- | --- | --- | --- | --- |
| `runtime:art.wall` | `wall-mounted` | `wall` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:bin.waste` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:bookshelf.low` | `surface-furniture` | `floor` | `derive-composite` | Reviewed active pixels: usable visible material exists, but support, base, or foreground semantics are baked together. | Reuse the accepted pixels where possible and derive deterministic support, underframe/base, and foreground occlusion parts. |
| `runtime:bookshelf.magazine` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:box.parcel` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:cabinet.filing` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:camera.cctv` | `wall-mounted` | `wall` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:camera.tripod` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:chair.cafe.down` | `seat` | `floor` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:chair.cafe.side` | `seat` | `floor` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:chair.cafe.up` | `seat` | `floor` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:chair.office.side` | `seat` | `floor` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:chair.office.up` | `seat` | `floor` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:chair.studio.up` | `seat` | `floor` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:clock.wall` | `wall-mounted` | `wall` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:counter.coffee` | `surface-furniture` | `floor` | `derive-composite` | Reviewed active pixels: usable visible material exists, but support, base, or foreground semantics are baked together. | Reuse the accepted pixels where possible and derive deterministic support, underframe/base, and foreground occlusion parts. |
| `runtime:cup.coffee` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:desk.creative.up` | `surface-furniture` | `floor` | `regenerate` | Reviewed active core-furniture pixels: the legacy desk top and depth do not match the canonical workstation contract. | Regenerate after Step 4 because the visible bitmap cannot represent the approved 5 x 4 footprint and 5 x 3 support plane. |
| `runtime:desk.noc.up` | `surface-furniture` | `floor` | `regenerate` | Reviewed active core-furniture pixels: the legacy desk top and depth do not match the canonical workstation contract. | Regenerate after Step 4 because the visible bitmap cannot represent the approved 5 x 4 footprint and 5 x 3 support plane. |
| `runtime:desk.standard.up` | `surface-furniture` | `floor` | `regenerate` | Reviewed active core-furniture pixels: the legacy desk top and depth do not match the canonical workstation contract. | Regenerate after Step 4 because the visible bitmap cannot represent the approved 5 x 4 footprint and 5 x 3 support plane. |
| `runtime:dispenser.water` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:divider.planter` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:door.closed` | `structural-opening` | `wall` | `derive-composite` | Reviewed active pixels: usable visible material exists, but support, base, or foreground semantics are baked together. | Reuse the accepted pixels where possible and derive deterministic support, underframe/base, and foreground occlusion parts. |
| `runtime:extinguisher.wall` | `wall-mounted` | `wall` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:keyboard.mouse` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:lamp.desk` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:lamp.floor` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:laptop.open` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:light.studio` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:machine.coffee` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:monitor.dual.active` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:monitor.front.active` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:network.stack` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:papers.stack` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:pet-bed.round` | `seat` | `floor` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:phone.preview` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:plant.potted` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:plant.small` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:plant.tall` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:printer.desktop` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:server.rack` | `upright-floor-object` | `floor` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:sign.exit` | `wall-mounted` | `wall` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:sofa.sectional` | `seat` | `floor` | `derive-composite` | Reviewed active pixels: usable visible material exists, but support, base, or foreground semantics are baked together. | Reuse the accepted pixels where possible and derive deterministic support, underframe/base, and foreground occlusion parts. |
| `runtime:station.multidevice` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:stool.lounge` | `seat` | `floor` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:table.cafe.round` | `surface-furniture` | `floor` | `derive-composite` | Reviewed active pixels: usable visible material exists, but support, base, or foreground semantics are baked together. | Reuse the accepted pixels where possible and derive deterministic support, underframe/base, and foreground occlusion parts. |
| `runtime:table.coffee` | `surface-furniture` | `floor` | `derive-composite` | Reviewed active pixels: usable visible material exists, but support, base, or foreground semantics are baked together. | Reuse the accepted pixels where possible and derive deterministic support, underframe/base, and foreground occlusion parts. |
| `runtime:table.meeting.empty` | `surface-furniture` | `floor` | `derive-composite` | Reviewed active pixels: usable visible material exists, but support, base, or foreground semantics are baked together. | Reuse the accepted pixels where possible and derive deterministic support, underframe/base, and foreground occlusion parts. |
| `runtime:tablet.drawing` | `upright-floor-object` | `furniture-surface` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |
| `runtime:tv.wall` | `wall-mounted` | `wall` | `metadata-fix` | Reviewed the active processed pixels and source sheet: the visible base is reusable after Geometry v3 metadata is supplied. | Keep the pixels and add explicit asset type, placement plane, pivots, bounds, overflow, and slot metadata. |

## Regeneration authorization

This audit does not authorize production generation. The accepted Camera/Scale
Bible remains the final Step 4 gate before any replacement image is created.
